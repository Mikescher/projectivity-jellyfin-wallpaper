<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    tools:ignore="MissingLeanbackLauncher">

    <uses-permission android:name="android.permission.INTERNET" />

    <uses-feature
        android:name="android.hardware.touchscreen"
        android:required="false" />
    <uses-feature
        android:name="android.software.leanback"
        android:required="true" />

    <!--
        Android 11+ package visibility: the launcher, so the refresh broadcast can be targeted at
        it, and every player the "Open with" setting can deep-link into.
    -->
    <queries>
        <package android:name="com.spocky.projengmenu" />
        <package android:name="com.github.damontecres.wholphin" />
        <package android:name="org.moonfin.androidtv" />
        <package android:name="org.jellyfin.androidtv" />
        <package android:name="org.jellyfin.androidtv.debug" />
        <package android:name="org.findroid" />
        <package android:name="tv.fladder" />
        <package android:name="org.jellyfin.mobile" />
    </queries>

    <application
        android:allowBackup="true"
        android:label="@string/plugin_name"
        android:supportsRtl="true"
        android:icon="@mipmap/ic_launcher"
        android:banner="@mipmap/ic_banner"
        android:usesCleartextTraffic="true"
        android:largeHeap="true"
        android:theme="@style/Theme.ProjectivyWallpaperProvider">

        <activity
            android:name=".SettingsActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.LAUNCHER" />
                <category android:name="android.intent.category.LEANBACK_LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".WallpaperProviderService"
            android:exported="true"
            android:label="@string/plugin_name"
            android:icon="@mipmap/ic_launcher"
            android:banner="@mipmap/ic_banner"
            tools:ignore="ExportedService">

            <!-- Mandatory for the plugin to be detected, DO NOT CHANGE -->
            <intent-filter>
                <action android:name="tv.projectivy.plugin.WALLPAPER_PROVIDER" />
            </intent-filter>

            <meta-data android:name="apiVersion" android:value="1" />
            <meta-data android:name="uuid" android:value="@string/plugin_uuid" />
            <meta-data android:name="name" android:value="@string/plugin_name" />
            <meta-data android:name="settingsActivity" android:value=".SettingsActivity" />
            <meta-data
                android:name="itemsCacheDurationMillis"
                android:value="@integer/items_cache_duration_millis" />

            <!-- TIME_ELAPSED | LAUNCHER_IDLE_MODE_CHANGED; card-focused events would fire
                 roughly once a second. -->
            <meta-data android:name="updateMode" android:value="17" />
        </service>

        <!--
            Serves composed wallpapers. Must be exported: Projectivy receives these URIs over AIDL
            rather than in an Intent, so there is no per-URI grant to hand it.
        -->
        <provider
            android:name=".WallpaperImageProvider"
            android:authorities="${applicationId}.provider"
            android:exported="true"
            android:grantUriPermissions="true" />

    </application>

</manifest>
